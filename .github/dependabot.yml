version: 2
updates:
  - package-ecosystem: "nuget"
    directories:
      - "**/*"
    schedule:
      interval: "daily"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
